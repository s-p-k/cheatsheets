# list packages that are no longer required as deps(orphans):
pacman -Qdt

# list official installed packages:
pacman -Qqn

# list package contents
pacman -Ql assaultcube | less

# (invalid or corrupted package (PGP signature)) error
pacman-key --refresh-keys && pacman -Syu
